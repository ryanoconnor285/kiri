export const KIRI_IMPORT_PROMPT = `Turn the notes below into flashcards for Kiri. Output only the cards, no commentary.

Format: first line is the front (question), following lines are the back (answer). Put a blank line between cards. Keep scientific terms readable; do not smash words together. If a formula appears, you may use KaTeX like $E = mc^2$ on its own line.

Notes:
\`\`\`
(paste notes here)
\`\`\`
`;
