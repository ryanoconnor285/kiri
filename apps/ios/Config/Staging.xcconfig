// Railway API service public URL (NOT the web app domain). No trailing slash.
// xcconfig treats // as a comment — use https:/$()/ for a proper https:// URL.
KIRI_API_URL = https:/$()/api-staging-7d58.up.railway.app
INFOPLIST_KEY_KIRIApiURL = $(KIRI_API_URL)
