services:
  postgres:
    image: postgres:16
    container_name: kiri-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kiri
    ports:
      - "5432:5432"
    volumes:
      - kiri_pgdata:/var/lib/postgresql/data

volumes:
  kiri_pgdata:
